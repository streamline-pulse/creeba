# Changesets

This folder is managed by [changesets](https://github.com/changesets/changesets).
Run `bun run changeset` to record a version bump; CI turns accumulated changesets
into a release PR and publishes on merge. See the changesets docs for details.
